version: '3'
services:
  odfe-node1:
    image: amazon/opendistro-for-elasticsearch:1.13.2
    container_name: odfe-node1
    environment:
      - cluster.name=odfe-cluster
      - node.name=odfe-node1
      - discovery.seed_hosts=odfe-node1,odfe-node2
      - cluster.initial_master_nodes=odfe-node1,odfe-node2
      - bootstrap.memory_lock=true # along with the memlock settings below, disables swapping
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m" # minimum and maximum Java heap size, recommend setting both to 50% of system RAM
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536 # maximum number of open files for the Elasticsearch user, set to at least 65536 on modern systems
        hard: 65536
    volumes:
      - odfe-data1:/usr/share/elasticsearch/data
      - ./openid(elastic)config.yaml:/usr/share/elasticsearch/plugins/opendistro_security/securityconfig/config.yml
      - ./openid(elastic)role_mapping.yaml:/usr/share/elasticsearch/plugins/opendistro_security/securityconfig/roles_mapping.yml
    ports:
      - 9200:9200
      - 9600:9600 # required for Performance Analyzer
    networks:
      - odfe-net
  odfe-node2:
    image: amazon/opendistro-for-elasticsearch:1.13.2
    container_name: odfe-node2
    environment:
      - cluster.name=odfe-cluster
      - node.name=odfe-node2
      - discovery.seed_hosts=odfe-node1,odfe-node2
      - cluster.initial_master_nodes=odfe-node1,odfe-node2
      - bootstrap.memory_lock=true
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    volumes:
      - odfe-data2:/usr/share/elasticsearch/data
      - ./openid(elastic)config.yaml:/usr/share/elasticsearch/plugins/opendistro_security/securityconfig/config.yml
      - ./openid(elastic)role_mapping.yaml:/usr/share/elasticsearch/plugins/opendistro_security/securityconfig/roles_mapping.yml
    networks:
      - odfe-net
  kibana:
    image: amazon/opendistro-for-elasticsearch-kibana:1.13.2
    container_name: odfe-kibana
    ports:
      - 5601:5601
    expose:
      - "5601"
    environment:
      ELASTICSEARCH_URL: https://odfe-node1:9200
      ELASTICSEARCH_HOSTS: https://odfe-node1:9200
    volumes:
      - ./openid(kibana).yaml:/usr/share/kibana/config/kibana.yml
    networks:
      - odfe-net
  # keycloak:
  #   image: quay.io/keycloak/keycloak:latest
  #   container_name: keycloak
  #   # hostname: keycloak
  #   # domainname: mylab.io
  #   environment:
  #     DB_VENDOR: h2
  #     KEYCLOAK_USER: admin
  #     KEYCLOAK_PASSWORD: admin
  #     KEYCLOAK_LOGLEVEL: ALL # ALL, DEBUG, ERROR, FATAL, INFO, OFF, TRACE and WARN
  #     KEYCLOAK_IMPORT: /tmp/realm.json    
  #   volumes:
  #     # - type: bind
  #     #   source: ./ssl/root-ca.pem
  #     #   target: /etc/x509/https/ca.crt
  #     # - type: bind
  #     #   source: ./ssl/keycloak.pem
  #     #   target: /etc/x509/https/tls.crt
  #     # - type: bind
  #     #   source: ./ssl/keycloak.key
  #     #   target: /etc/x509/https/tls.key
  #     - type: bind
  #       source: ./oauth2-keycloak.json
  #       target: /tmp/realm.json
  #   ports:
  #     - 8443:8443
  #     - 8080:8080
  #   expose:
  #     - "8443"
  #     - "8080"
  #   networks:
  #     - odfe-net
volumes:
  odfe-data1:
  odfe-data2:

networks:
  odfe-net: